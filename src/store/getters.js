export const apiIsValid = state => state.api.providers[state.api.selectedProvider].valid;

export const currentAppColor = state => state.search.color;

export const currentSearchTerm = state => state.search.term;
